{
  "source": "src/index.ts",
  "module": "dist/switcher-boiler-card.js",
  "targets": {
    "module": {
      "includeNodeModules": true
    }
  },
  "scripts": {
    "watch": "node -e \"console.log('Last build:', new Date().toLocaleString())\" && parcel watch",
    "build": "parcel build --no-source-maps"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.13.3",
    "parcel": "^2.13.3"
  },
  "dependencies": {
    "custom-card-helpers": "^1.9.0",
    "home-assistant-js-websocket": "^9.4.0",
    "lit": "^3.2.1"
  }
}
